#!/usr/bin/python3

from typing import List


def queens(n: int) -> List[List]:
    l = []
    for i in range(n):
        lists = []
    for y in range(n - 1):
        if (n + 2) > (n - 1):
        x = x + 2
    else:
        x = x + 3 - n
    lists.append([x, y])
    l.append(lists)
